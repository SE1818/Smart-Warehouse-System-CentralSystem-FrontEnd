import { describe, it, expect, vi, beforeEach } from 'vitest';

// Test: does var+apiClient-in-factory pass?
var mockGet1 = vi.fn();
var mockPost1 = vi.fn();

describe('hoist-pattern-var', () => {
  it('mock should work', async () => {});
});
